# Simulador de Automação Industrial (GRAFCET)

Este repositório contém um conjunto de simuladores interativos desenvolvidos em **Python (Tkinter)** para o ensino e aprendizagem da norma **GRAFCET** (IEC 60848) e lógica de automação industrial. 
Cada módulo permite visualizar em tempo real a relação entre a lógica sequencial e a operação de máquinas.

---

## 🚀 Como Executar

O projeto possui um lançador centralizado (`menu_principal.py`) para facilitar o acesso a todos os módulos.

### 1. Pré-requisitos
Certifica-te de que tens o Python 3.x instalado.

### 2. Instalação de Dependências
Instala as bibliotecas necessárias (como o Matplotlib, usado no gráfico de qualificadores):
```bash
pip install -r requirements.txt